# v63 Prompt Template Pack

프로젝트: {{ project_name }}
생성일: {{ generated_at }}
주 템플릿 엔진: {{ active_engine }}
선택 정책: {{ selected_template_policy }}

## 콘셉트
{{ concept_text }}

## 생성 지시 템플릿
- 캐릭터는 독창적인 소재/성격/말투로 구성한다.
- 기존 캐릭터명, 유명 IP, 유명인 이미지, 판매 중인 이모티콘 표현을 복제하지 않는다.
- 정지형과 움직이는형의 색상, 외곽선, 얼굴 비율을 유지한다.
- 움직이는형은 24개 구성 초안 중 대표 GIF 3개를 우선 미리보기로 만든다.

## 트렌드 신호
{% for item in trend_signals %}- {{ item }}
{% endfor %}

## 출력 목표
{% for item in intended_outputs %}- {{ item }}
{% endfor %}
